# Sample Docksmithfile — exercises all six instructions
FROM alpine:3.18

WORKDIR /app

ENV GREETING=Hello TARGET=Docksmith
ENV APP_VERSION=1.1.0
ENV EMPHASIS=green

COPY app.sh app.py /app/
COPY vendor/ /app/vendor/

RUN chmod +x /app/app.sh

CMD ["/bin/sh", "/app/app.sh"]
